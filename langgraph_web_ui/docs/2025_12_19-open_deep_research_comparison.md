     # Open Deep Research vs Our Deep Research 비교 분석

> 작성일: 2025-12-19
> 목적: LangChain 공식 Open Deep Research 프로젝트와 우리 프로젝트 아키텍처 비교

---

## 1. 개요

| 항목 | Open Deep Research | Our Project |
|------|---------------------|-------------|
| **개발 주체** | LangChain 공식 | 자체 개발 |
| **성숙도** | Production-ready | Learning/Prototype |
| **벤치마크** | Deep Research Bench #6 | 미측정 |
| **코드 규모** | ~30K+ lines | ~2K lines |

---

## 2. 아키텍처 비교

### Open Deep Research 그래프

```
┌──────────────────┐
│ clarify_with_user│ ← 사용자에게 명확화 질문
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│write_research_brief│ ← 리서치 브리프 생성
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────────────┐
│            SUPERVISOR SUBGRAPH               │
│  ┌──────────┐      ┌──────────────────┐      │
│  │supervisor│ ←──▶ │ supervisor_tools │      │
│  └──────────┘      └──────────────────┘      │
│       │                                       │
│       ▼ (ConductResearch 호출)               │
│  ┌──────────────────────────────────────┐    │
│  │        RESEARCHER SUBGRAPH           │    │
│  │  ┌──────────┐  ┌──────────────────┐  │    │
│  │  │researcher│──│ researcher_tools │  │    │
│  │  └──────────┘  └──────────────────┘  │    │
│  │        │                              │    │
│  │        ▼                              │    │
│  │  ┌──────────────────┐                 │    │
│  │  │ compress_research│ ← 연구 압축     │    │
│  │  └──────────────────┘                 │    │
│  └──────────────────────────────────────┘    │
└──────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────┐
│ final_report_generation│ ← 최종 보고서 생성
└────────────────────────┘
```

### Our Project 그래프

```
┌──────────┐
│ Planner  │ ← 검색 쿼리 계획
└────┬─────┘
     │
     ▼
┌──────────┐
│ Searcher │ ← Tavily 검색
└────┬─────┘
     │
     ▼
┌──────────────┐
│ContentReader │ ← URL 내용 읽기
└──────┬───────┘
       │
       ▼
┌──────────┐
│ Analyzer │ ← 분석 + 추가 검색 결정
└────┬─────┘
     │  ↑
     │  └─── (needs_more_research)
     ▼
┌──────────┐
│  Writer  │ ← 최종 응답
└──────────┘
```

---

## 3. 핵심 기능 비교

### 3.1 LLM 사용

| 기능 | Open Deep Research | Our Project |
|------|---------------------|-------------|
| **모델 수** | 4개 (역할별 분리) | 1개 (공유) |
| **Summarization** | gpt-4.1-mini | gemini-2.0-flash |
| **Research** | gpt-4.1 | gemini-2.0-flash |
| **Compression** | gpt-4.1 | ❌ 없음 |
| **Final Report** | gpt-4.1 | gemini-2.0-flash |
| **설정 가능성** | ✅ 환경변수로 변경 | ❌ 하드코딩 |

**우리 프로젝트 부족한 점:**
- 단일 LLM 사용으로 역할별 최적화 불가
- 토큰 비용 최적화 어려움 (작은 모델/큰 모델 분리 못함)

---

### 3.2 검색 API

| 기능 | Open Deep Research | Our Project |
|------|---------------------|-------------|
| **Tavily** | ✅ 지원 | ✅ 지원 |
| **OpenAI 웹 검색** | ✅ 네이티브 지원 | ❌ 미지원 |
| **Anthropic 웹 검색** | ✅ 네이티브 지원 | ❌ 미지원 |
| **MCP 서버** | ✅ 전체 지원 | ❌ 미지원 |
| **검색 API 선택** | ✅ 설정으로 변경 | ❌ Tavily 고정 |

**우리 프로젝트 부족한 점:**
- Tavily만 사용 가능
- MCP(Model Context Protocol) 미지원

---

### 3.3 사용자 상호작용

| 기능 | Open Deep Research | Our Project |
|------|---------------------|-------------|
| **명확화 질문** | ✅ clarify_with_user 노드 | ❌ 없음 |
| **리서치 브리프** | ✅ 체계적 변환 | ❌ 없음 |
| **언어 감지** | ✅ 입력 언어로 응답 | ⚠️ 프롬프트로만 |

**우리 프로젝트 부족한 점:**
- 사용자에게 명확화 질문 기능 없음
- 모호한 질문 그대로 처리

---

### 3.4 연구 관리

| 기능 | Open Deep Research | Our Project |
|------|---------------------|-------------|
| **병렬 연구** | ✅ 최대 20개 병렬 | ❌ 순차 처리 |
| **연구 압축** | ✅ compress_research | ❌ 없음 |
| **반복 제한** | ✅ 6회 (설정 가능) | ✅ 3회 (하드코딩) |
| **도구 호출 제한** | ✅ 10회 (설정 가능) | ❌ 없음 |
| **Think Tool** | ✅ 전략적 사고 도구 | ❌ 없음 |

**우리 프로젝트 부족한 점:**
- 병렬 처리 미지원 → 느린 처리 속도
- 연구 결과 압축 없음 → 토큰 낭비
- Think Tool 없음 → 전략적 계획 부재

---

### 3.5 프롬프트 품질

| 항목 | Open Deep Research | Our Project |
|------|---------------------|-------------|
| **프롬프트 길이** | 21K bytes (상세) | ~2K bytes (간단) |
| **구조화** | XML 태그 사용 | 일반 텍스트 |
| **예시 포함** | ✅ 다수의 예시 | ⚠️ 최소한 |
| **규칙 명시** | ✅ Hard Limits, Guidelines | ⚠️ 기본 지침만 |
| **인용 규칙** | ✅ 상세한 Citation Rules | ❌ 없음 |

**Open Deep Research 프롬프트 예시:**
```xml
<Task>
Your job is to use tools to gather information...
</Task>

<Hard Limits>
**Tool Call Budgets**:
- Simple queries: 2-3 maximum
- Complex queries: up to 5 maximum
</Hard Limits>

<Show Your Thinking>
After each search, use think_tool to analyze:
- What key information did I find?
- What's missing?
</Show Your Thinking>
```

**우리 프로젝트 프롬프트 예시:**
```
You are a RESEARCH PLANNER. Your job is to create a research strategy.

Analyze the user's question and create a research plan with:
1. Multiple search queries
2. Focus areas to explore
3. Depth level (1=quick, 2=medium, 3=deep)
```

**우리 프로젝트 부족한 점:**
- 프롬프트 구조화 부족
- 예시와 가이드라인 부족
- 인용 규칙 없음

---

### 3.6 State 관리

| 항목 | Open Deep Research | Our Project |
|------|---------------------|-------------|
| **State 클래스 수** | 5개 | 1개 |
| **Pydantic 사용** | ✅ 타입 검증 | ❌ TypedDict만 |
| **서브그래프 State** | ✅ 독립적 State | ❌ 없음 |
| **Override Reducer** | ✅ 커스텀 리듀서 | ❌ 기본만 |

**Open Deep Research State 구조:**
```python
class AgentState(MessagesState):
    supervisor_messages: Annotated[list, override_reducer]
    research_brief: Optional[str]
    raw_notes: Annotated[list[str], override_reducer]
    notes: Annotated[list[str], override_reducer]
    final_report: str

class SupervisorState(TypedDict):
    research_iterations: int = 0
    ...

class ResearcherState(TypedDict):
    tool_call_iterations: int = 0
    compressed_research: str
    ...
```

**우리 프로젝트 부족한 점:**
- 단일 State로 모든 것 관리 → 복잡도 증가
- 서브그래프 간 State 분리 없음

---

### 3.7 설정 관리

| 항목 | Open Deep Research | Our Project |
|------|---------------------|-------------|
| **Configuration 클래스** | ✅ Pydantic 기반 | ❌ 없음 |
| **환경 변수** | ✅ 모든 설정 가능 | ⚠️ API 키만 |
| **UI 메타데이터** | ✅ x_oap_ui_config | ❌ 없음 |
| **런타임 설정** | ✅ RunnableConfig | ❌ 없음 |

---

## 4. 우리 프로젝트의 장점

| 장점 | 설명 |
|------|------|
| **단순함** | 이해하기 쉬운 5노드 구조 |
| **학습 용이** | LangGraph 입문에 적합 |
| **빠른 프로토타이핑** | 빠르게 테스트 가능 |
| **비용 효율** | 단일 모델로 저비용 |
| **한국어 최적화** | 한국어 응답에 집중 |

---

## 5. 개선 우선순위

### 🔴 High Priority (필수)

| 항목 | 현재 | 목표 |
|------|------|------|
| **연구 압축** | ❌ 없음 | compress_research 추가 |
| **프롬프트 강화** | 간단 | XML 구조화, 예시 추가 |
| **인용 규칙** | ❌ 없음 | Citation Rules 추가 |

### 🟡 Medium Priority (권장)

| 항목 | 현재 | 목표 |
|------|------|------|
| **명확화 질문** | ❌ 없음 | clarify_with_user 추가 |
| **Think Tool** | ❌ 없음 | 전략적 사고 도구 추가 |
| **Configuration** | 하드코딩 | 설정 클래스 추가 |
| **다중 LLM** | 1개 | 역할별 LLM 분리 |

### 🟢 Low Priority (선택)

| 항목 | 현재 | 목표 |
|------|------|------|
| **병렬 연구** | 순차 | 병렬 처리 |
| **MCP 지원** | ❌ | MCP 통합 |
| **서브그래프** | ❌ | 모듈화된 서브그래프 |

---

## 6. 구현 권장 사항

### 6.1 compress_research 노드 추가

```python
def compress_research_node(state: DeepResearchState) -> dict:
    """수집된 연구 결과를 압축하여 토큰 절약"""
    
    COMPRESS_PROMPT = """
    Clean up and compress the research findings:
    1. Remove duplicate information
    2. Preserve key facts and sources
    3. Structure in clean format
    """
    
    raw_contents = state.get("read_contents", [])
    compressed = llm.invoke(COMPRESS_PROMPT + str(raw_contents))
    
    return {"compressed_notes": compressed.content}
```

### 6.2 명확화 질문 노드 추가

```python
def clarify_with_user_node(state: DeepResearchState) -> dict:
    """사용자에게 명확화 질문"""
    
    CLARIFY_PROMPT = """
    Assess if you need to ask a clarifying question.
    If there are acronyms, abbreviations, or unclear terms, ask.
    
    Output JSON:
    {"need_clarification": bool, "question": str}
    """
    ...
```

### 6.3 프롬프트 구조화

```python
RESEARCHER_PROMPT = """
<Task>
Your job is to search and gather information about the topic.
</Task>

<Available Tools>
1. tavily_search: Web search
2. read_url_tool: Read full URL content
3. think_tool: Strategic reflection
</Available Tools>

<Hard Limits>
- Maximum 5 search tool calls
- Stop when you have 3+ relevant sources
</Hard Limits>

<Show Your Thinking>
After each search, analyze:
- What key information did I find?
- What's still missing?
- Should I continue or stop?
</Show Your Thinking>
"""
```

---

## 7. 결론

| 관점 | Open Deep Research | Our Project |
|------|---------------------|-------------|
| **목적** | Production-ready | Learning/Prototype |
| **강점** | 완성도, 확장성, 성능 | 단순함, 이해 용이 |
| **약점** | 복잡도 높음 | 기능 제한 |
| **적합 대상** | 실제 서비스 | 학습, 빠른 테스트 |

> 💡 **결론**: Open Deep Research는 "프로덕션 수준의 완성된 솔루션"이고,
> 우리 프로젝트는 "LangGraph 학습 및 프로토타이핑에 적합한 간소화된 구현"입니다.
> 
> 점진적으로 compress_research, clarify_with_user, 프롬프트 강화 등을 추가하면
> 우리 프로젝트도 상당한 수준으로 발전시킬 수 있습니다.
