# Agentic Insight Dashboard - Enhancement Design Specification

## Overview

이 문서는 사용자 피드백을 반영하여 다음 항목을 개선하기 위한 설계서입니다:
1. **프로젝트 이름 변경**: "Gemini" → "Agentic Insight"
2. **사이드바 재설계**: Gemini 플랫폼 스타일에 맞게 개선
3. **Live Graph 위치 재배치**: 더 적절한 위치 선정
4. **세션 관리 시스템**: DB 필요 여부 및 구현 방안

---

## 1. 프로젝트 네이밍 변경

### 변경 사항
| 현재 | 변경 후 |
|---|---|
| "Gemini" | "Agentic Insight" |
| "Gemini Agent" | "AI Supervisor" |
| 그라디언트 아바타 (Gemini 스타일) | 네트워크/뇌 아이콘 + 보라색 그라디언트 |

### 영향 파일
- `App.vue`: 헤더, 웰컴 메시지
- `ChatMessage.vue`: AI 아바타, 라벨

---

## 2. 사이드바 재설계 (Gemini 스타일)

### 현재 문제점
- 사이드바에 Live Graph가 너무 작게 표시됨
- 채팅 히스토리가 더미 데이터로 채워짐
- 레이아웃이 어색함

### Gemini 스타일 사이드바 구조
```
┌─────────────────────────┐
│ [≡] Agentic Insight     │  ← 로고 + 토글
├─────────────────────────┤
│ [+] 새 채팅              │  ← New Chat 버튼
├─────────────────────────┤
│ 최근 채팅               │  ← 섹션 헤더
│ ○ AI 에이전트 연구      │  ← 세션 항목
│ ○ LangGraph 패턴        │
│ ○ FastAPI 스트리밍      │
├─────────────────────────┤
│ [설정] [도움말]         │  ← 하단 액션
└─────────────────────────┘
```

### 제안: Live Graph 위치 옵션

**옵션 A: 별도 패널 (오른쪽)**
```
┌────────────┬────────────────────┬──────────────┐
│  Sidebar   │   Chat (Center)    │ Live Graph   │
│  (280px)   │   (flex-1)         │   (320px)    │
└────────────┴────────────────────┴──────────────┘
```
- 장점: 그래프가 항상 보이고 크기가 충분함
- 단점: 화면 공간이 더 필요함 (1440px+ 권장)

**옵션 B: 채팅 영역 상단 콜랩서블 (추천)**
```
┌────────────┬────────────────────────────────────┐
│  Sidebar   │  [▼ Live Graph Execution]          │  ← 접을 수 있는 패널
│  (280px)   ├────────────────────────────────────┤
│            │          Chat Messages             │
│            ├────────────────────────────────────┤
│            │        [Input Field]               │
└────────────┴────────────────────────────────────┘
```
- 장점: 필요할 때만 펼쳐서 볼 수 있음, 화면 효율적
- 단점: 항상 보이지 않음

**옵션 C: 플로팅 미니 위젯 (현재 Gemini 스타일)**
```
┌────────────┬────────────────────────────────────┐
│  Sidebar   │                              ┌───┐ │
│            │      Chat Messages           │ G │ │  ← 오른쪽 하단 미니 그래프
│            │                              └───┘ │
└────────────┴────────────────────────────────────┘
```
- 장점: 비침투적, 필요시 클릭하여 확대
- 단점: 기본 상태에서 정보가 제한적

---

## 3. 세션 관리 시스템

### 질문: DB가 필요한가?

**답변: 프로토타입 단계에서는 불필요, 프로덕션에서는 권장**

### 단계별 구현 방안

#### Phase 1: 클라이언트 사이드 (현재 가능)
```typescript
// localStorage 기반 세션 저장
interface Session {
  id: string;
  title: string;
  messages: Message[];
  createdAt: number;
  updatedAt: number;
}

// Pinia Store 확장
const sessions = ref<Session[]>([]);
const currentSessionId = ref<string | null>(null);

// localStorage에 자동 저장
watch(sessions, (newSessions) => {
  localStorage.setItem('agentic_sessions', JSON.stringify(newSessions));
}, { deep: true });
```
- **장점**: 서버 변경 없음, 빠른 구현
- **단점**: 브라우저 간 동기화 불가, 용량 제한 (5MB)

#### Phase 2: 서버 사이드 (SQLite)
```python
# server/models.py
from sqlalchemy import Column, String, Text, DateTime
from sqlalchemy.ext.declarative import declarative_base

Base = declarative_base()

class Session(Base):
    __tablename__ = "sessions"
    id = Column(String, primary_key=True)
    title = Column(String)
    messages = Column(Text)  # JSON serialized
    created_at = Column(DateTime)
    updated_at = Column(DateTime)
```
- **장점**: 영구 저장, 서버 간 공유 가능
- **단점**: 추가 의존성 (SQLAlchemy), 마이그레이션 필요

#### Phase 3: 클라우드 DB (PostgreSQL/Supabase)
- 프로덕션 배포 시 고려

### 추천: Phase 1 (localStorage) 먼저 구현

---

## 4. 구현 체크리스트

### 즉시 적용 (High Priority)
- [ ] **Rename**: "Gemini" → "Agentic Insight" 전체 교체
- [ ] **Sidebar**: Live Graph 제거, 채팅 히스토리 UI만 유지
- [ ] **Live Graph**: 옵션 B (상단 콜랩서블 패널)로 이동

### 단기 적용 (Medium Priority)
- [ ] **Session Store**: localStorage 기반 세션 저장 구현
- [ ] **New Chat**: 새 채팅 버튼 기능 구현
- [ ] **Session List**: 사이드바에 저장된 세션 목록 표시

### 장기 고려 (Low Priority)
- [ ] **Server DB**: SQLite/PostgreSQL 세션 저장
- [ ] **Search**: 세션 검색 기능
- [ ] **Export**: 채팅 내역 내보내기

---

## 5. UI 목업 (최종 레이아웃)

```
┌─────────────────────────────────────────────────────────────────────┐
│ [≡]  Agentic Insight                              [Settings] [👤]   │
├──────────────┬──────────────────────────────────────────────────────┤
│              │ ┌──────────────────────────────────────────────────┐ │
│  Sidebar     │ │ [▼ Live Graph]  Supervisor → Researcher → ...   │ │ ← 접을 수 있음
│  ──────────  │ └──────────────────────────────────────────────────┘ │
│  [+] 새 채팅  │                                                      │
│              │      "안녕하세요, Agentic Insight입니다."            │
│  최근        │      "무엇을 도와드릴까요?"                          │
│  ○ 세션 1    │                                                      │
│  ○ 세션 2    │      [  AI 에이전트 연구  ] [  LangGraph  ]          │ ← 추천 칩
│  ○ 세션 3    │                                                      │
│              ├──────────────────────────────────────────────────────┤
│              │ ┌──────────────────────────────────────────────────┐ │
│  [설정]      │ │           [여기에 프롬프트를 입력하세요] ⬆️       │ │
│              │ └──────────────────────────────────────────────────┘ │
└──────────────┴──────────────────────────────────────────────────────┘
```
