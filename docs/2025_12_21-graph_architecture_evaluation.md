# 그래프 아키텍처 평가 및 최적화 방향

> 작성일: 2025-12-21
> 목적: 그래프 전문가 관점에서 아키텍처 분석 및 최적화 방향 제시

---

## 1. 아키텍처 형태 분석

### 우리 프로젝트: 선형(Linear) 그래프

```
┌─────────┐   ┌─────────┐   ┌──────────┐   ┌──────────────┐
│ Clarify │ → │ Planner │ → │ Searcher │ → │ContentReader │
└─────────┘   └─────────┘   └──────────┘   └──────────────┘
                                                  │
     ┌────────────────────────────────────────────┘
     ▼
┌──────────┐   ┌──────────┐   ┌────────┐   ┌──────────┐
│ Analyzer │ → │ Compress │ → │ Writer │ → │ Critique │
└──────────┘   └──────────┘   └────────┘   └──────────┘
     │
     └─────── (loop back to Searcher)
```

**특징**: 노드들이 **일렬로 연결**된 단순한 구조

---

### Open Deep Research: 계층형(Hierarchical) 그래프

```
┌──────────────────┐
│ clarify_with_user│
└────────┬─────────┘
         ▼
┌──────────────────────────────────────────────────────┐
│              SUPERVISOR SUBGRAPH                      │
│  ┌───────────┐                                        │
│  │ Supervisor│ ─── ConductResearch() ───┐            │
│  └───────────┘                          ▼            │
│                    ┌────────────────────────────┐    │
│                    │   RESEARCHER SUBGRAPH #1   │    │
│                    │   Search → Analyze → Compress   │
│                    └────────────────────────────┘    │
│                    ┌────────────────────────────┐    │
│                    │   RESEARCHER SUBGRAPH #2   │ ←─ 병렬!
│                    │   Search → Analyze → Compress   │
│                    └────────────────────────────┘    │
│                    ┌────────────────────────────┐    │
│                    │   RESEARCHER SUBGRAPH #N   │    │
│                    │   Search → Analyze → Compress   │
│                    └────────────────────────────┘    │
└──────────────────────────────────────────────────────┘
         ▼
┌────────────────────────┐
│ final_report_generation│
└────────────────────────┘
```

**특징**: **서브그래프 + Supervisor 패턴** + **병렬 실행**

---

## 2. 장단점 비교

### 우리 프로젝트 (선형 그래프)

| 장점 | 설명 |
|------|------|
| ✅ **단순함** | 이해하기 쉬움, 디버깅 용이 |
| ✅ **예측 가능** | 실행 순서가 명확 |
| ✅ **낮은 복잡도** | 코드 유지보수 쉬움 |
| ✅ **반복 연구 지원** | Analyzer → Searcher 루프로 깊이 있는 연구 |

| 단점 | 설명 |
|------|------|
| ❌ **병렬 처리 불가** | 모든 노드가 순차 실행 |
| ❌ **확장성 제한** | 노드 추가 시 전체 흐름 영향 |
| ❌ **유연성 부족** | 동적 분기 어려움 |
| ❌ **시간 비효율** | 독립적 작업도 순차 처리 |

---

### Open Deep Research (계층형 그래프)

| 장점 | 설명 |
|------|------|
| ✅ **병렬 처리** | 여러 Researcher 동시 실행 (최대 20개) |
| ✅ **모듈성** | 서브그래프 재사용 가능 |
| ✅ **확장성** | 새 서브그래프 추가 용이 |
| ✅ **동적 분기** | Supervisor가 상황에 맞게 결정 |
| ✅ **관심사 분리** | 각 서브그래프가 독립적 책임 |

| 단점 | 설명 |
|------|------|
| ❌ **복잡도** | 이해/디버깅 어려움 |
| ❌ **오버헤드** | 서브그래프 간 통신 비용 |
| ❌ **State 관리** | 여러 State 클래스 동기화 필요 |
| ❌ **학습 곡선** | LangGraph 고급 기능 필요 |

---

## 3. 왜 우리 그래프가 "최적화되지 않은" 것처럼 보이는가?

### 3.1 병목 지점 분석

```
┌─────────┐     ┌─────────┐     ┌──────────┐     ┌──────────────┐
│ Clarify │ ──→ │ Planner │ ──→ │ Searcher │ ──→ │ContentReader │
└─────────┘     └─────────┘     └──────────┘     └──────────────┘
   ~1초            ~3초            ~2초             ~2초 (병렬화됨)
                                   ↑
                            병목 1: LLM 호출
                            
     ┌──────────┐     ┌──────────┐     ┌────────┐     ┌──────────┐
  →  │ Analyzer │ ──→ │ Compress │ ──→ │ Writer │ ──→ │ Critique │
     └──────────┘     └──────────┘     └────────┘     └──────────┘
        ~4초             ~3초            ~8초            ~3초
         ↑                                 ↑               ↑
   병목 2: 분석          병목 3: 작성    병목 4: 평가
```

**핵심 병목**: 
1. **LLM 호출이 순차적** - 모든 노드가 LLM을 사용하지만 하나씩 실행
2. **독립 작업도 대기** - Clarify와 Planner는 동시에 가능하나 순차 실행
3. **반복 루프가 전체를 지연** - 추가 검색 시 전체 파이프라인 재실행

---

### 3.2 Open Deep Research와의 핵심 차이

| 관점 | 우리 프로젝트 | Open Deep Research |
|------|--------------|-------------------|
| **그래프 깊이** | 8노드 직렬 | 3노드 + 서브그래프 |
| **실행 모델** | Sequential | Parallel + Hierarchical |
| **제어권** | 고정된 흐름 | Supervisor 동적 결정 |
| **확장 방식** | 노드 추가 | 서브그래프 추가 |

---

## 4. 최적화 방향

### 4.1 단기 최적화 (현재 구조 유지)

```
현재: Clarify → Planner → Searcher → ContentReader → ...

개선 1: Clarify와 Planner 병합
        ┌──────────────┐
        │Clarify+Planner│ → Searcher → ...
        └──────────────┘
        (LLM 호출 1회 감소)

개선 2: ContentReader + 일부 분석 병합
        Searcher → ┌──────────────────┐
                   │ContentReader+Analyze│ → Compress → ...
                   └──────────────────┘
```

**예상 효과**: 10-15% 시간 단축

---

### 4.2 중기 최적화 (서브그래프 도입)

```
┌─────────────────────────────────────────────────┐
│          RESEARCH SUBGRAPH                       │
│  Searcher → ContentReader → Analyzer            │
│      ↑          │                               │
│      └──────────┘ (needs_more_research)          │
└─────────────────────────────────────────────────┘

메인 그래프:
Clarify+Planner → [RESEARCH SUBGRAPH] → Compress → Writer → Critique
```

**장점**:
- 연구 로직 캡슐화
- 테스트/디버깅 용이
- 향후 병렬 연구 확장 기반

---

### 4.3 장기 최적화 (Supervisor 패턴)

```
┌──────────────────────────────────────────────────────┐
│                 SUPERVISOR                            │
│  - 쿼리 분석                                          │
│  - 필요한 연구 수 결정                                │
│  - 병렬 연구 시작                                     │
└─────────────────────┬────────────────────────────────┘
                      │
          ┌───────────┼───────────┐
          ▼           ▼           ▼
     [Research 1] [Research 2] [Research 3]
          │           │           │
          └───────────┼───────────┘
                      ▼
              ┌──────────────┐
              │ Report Gen   │
              └──────────────┘
```

**효과**: 
- 처리 시간 **50%+ 단축** 가능
- 복잡한 쿼리에 더 많은 연구 할당

---

## 5. 권장 구현 순서

### Phase 8: 서브그래프 도입

```python
# 연구 서브그래프 정의
research_subgraph = StateGraph(ResearchState)
research_subgraph.add_node("Searcher", searcher_node)
research_subgraph.add_node("ContentReader", content_reader_node)
research_subgraph.add_node("Analyzer", analyzer_node)
research_subgraph.add_edge("Searcher", "ContentReader")
research_subgraph.add_edge("ContentReader", "Analyzer")
research_subgraph.add_conditional_edges("Analyzer", should_continue, {...})

# 메인 그래프에서 서브그래프 사용
main_graph.add_node("Research", research_subgraph.compile())
```

### Phase 9: Supervisor 패턴

```python
# Supervisor 노드
def supervisor_node(state):
    # 쿼리 복잡도 분석
    complexity = analyze_complexity(state["query"])
    
    # 병렬 연구 수 결정
    num_researches = min(complexity, MAX_PARALLEL)
    
    # Send API로 병렬 실행
    return [
        Send("Research", {"query": q, "id": i})
        for i, q in enumerate(queries[:num_researches])
    ]
```

---

## 6. 결론

| 현재 상태 | 최적화 후 |
|----------|----------|
| 선형 8노드 그래프 | 계층형 서브그래프 |
| 순차 실행만 | 병렬 + 순차 혼합 |
| ~30초 | **~15초 (목표)** |

> 💡 **핵심 인사이트**: 
> 
> 우리 그래프가 "비최적화"된 것은 **설계 결함이 아니라 학습 단계의 자연스러운 결과**입니다.
> 
> 선형 그래프는 **이해하기 쉽고 안정적**이지만, 성능 최적화를 위해서는 
> **서브그래프 + Supervisor 패턴**으로 진화해야 합니다.
>
> 다음 단계:
> 1. Phase 8: 연구 서브그래프 분리
> 2. Phase 9: Supervisor 패턴 도입
> 3. Phase 10: LangGraph `Send` API로 병렬 실행
