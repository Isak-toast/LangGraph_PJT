# Agentic Insight Dashboard

**Agentic Insight Dashboard**는 AI 에이전트들이 협업하여 리서치 작업을 수행하는 과정을 실시간으로 시각화하고 제어할 수 있는 웹 애플리케이션입니다.

LangGraph의 **Multi-Agent Supervisor** 패턴을 기반으로 하며, Vue 3와 FastAPI를 사용하여 모던한 웹 인터페이스를 제공합니다.

---

## ✨ 주요 기능 (Features)

1.  **멀티 에이전트 협업 (Multi-Agent Collaboration)**
    -   **Supervisor**: 사용자의 요청을 분석하여 적절한 작업자(Worker)에게 할당합니다.
    -   **Researcher**: Tavily API를 사용하여 최신 웹 정보를 검색합니다.
    -   **Writer**: 검색된 정보를 바탕으로 보고서를 작성하거나 요약합니다.

2.  **실시간 시각화 (Real-time Visualization)**
    -   현재 실행 중인 LangGraph의 상태와 에이전트 간의 흐름을 **Mermaid 다이어그램**으로 실시간으로 보여줍니다.
    -   활성화된 노드(Agent)가 하이라이트되어 실행 과정을 직관적으로 이해할 수 있습니다.

3.  **스트리밍 응답 (Streaming Responses)**
    -   LLM의 생성 토큰을 실시간으로 스트리밍하여, 마치 사람이 타이핑하는 듯한 경험을 제공합니다.
    -   에이전트의 내부 사고 과정(Node 진입/종료)도 로그로 확인할 수 있습니다.

---

## 🚀 시작하기 (Getting Started)

### 1. 환경 설정 (Prerequisites)

-   Python 3.10 이상
-   Node.js 18 이상
-   **API 키 설정**: 프로젝트 루트(`../../.env`) 또는 `server/.env`에 다음 키가 필요합니다.

```ini
GOOGLE_API_KEY=your_google_api_key
TAVILY_API_KEY=your_tavily_api_key
```

### 2. 백엔드 실행 (Backend)

`langgraph_web_ui/server` 디렉토리에서 실행합니다.

```bash
# 1. 가상환경 생성 및 활성화
cd langgraph_web_ui/server
python3 -m venv venv
source venv/bin/activate

# 2. 의존성 설치
pip install -r requirements.txt

# 3. 서버 실행
python app.py
```
*서버는 `http://localhost:8000`에서 시작됩니다.*

### 3. 프론트엔드 실행 (Frontend)

`langgraph_web_ui/client` 디렉토리에서 실행합니다.

```bash
cd langgraph_web_ui/client

# 1. 의존성 설치
npm install

# 2. 개발 서버 실행
npm run dev
```
*브라우저에서 `http://localhost:5173`으로 접속하세요.*

---

## 💡 사용 예시 (Usage)

웹 인터페이스의 채팅창에 다음과 같은 질문을 던져보세요.

### 1. 최신 기술 트렌드 조사
> "2024년 생성형 AI의 최신 트렌드에 대해 조사하고 요약해줘."

**실행 흐름:**
1.  **Supervisor**가 요청을 받고 `Researcher`에게 검색을 지시합니다.
2.  **Researcher**가 웹 검색을 수행하고 결과를 반환합니다.
3.  **Supervisor**가 결과를 확인하고 `Writer`에게 요약을 지시합니다.
4.  **Writer**가 최종 답변을 작성합니다.

### 2. 특정 주제 심층 분석
> "LangGraph의 주요 기능과 장점에 대해 조사해서 보고서 형식으로 써줘."

---

## 🛠️ 기술 스택 (Tech Stack)

-   **Frontend**: Vue 3, TypeScript, Tailwind CSS, shadcn-vue, Pinia, Mermaid.js
-   **Backend**: FastAPI, LangGraph, LangChain, Server-Sent Events (SSE)
-   **AI Model**: Google Gemini Pro (via `langchain-google-genai`)
